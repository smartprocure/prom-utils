{
  "name": "p-utils",
  "version": "0.1.0",
  "description": "Promise utilities for looping: rate limiting, queueing/batching, etc.",
  "author": "GovSpend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": "git://github.com/smartprocure/p-utils.git",
  "scripts": {
    "prepare": "npm run lint && npm test && npm run build",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc --declaration",
    "test": "jest",
    "lint": "eslint src/**"
  },
  "keywords": [
    "promise",
    "rate",
    "limit",
    "batch",
    "queue",
    "concurrency",
    "control"
  ],
  "license": "ISC",
  "prettier": {
    "semi": false,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "devDependencies": {
    "@types/jest": "^28.1.1",
    "@types/node": "^17.0.44",
    "@typescript-eslint/eslint-plugin": "^5.28.0",
    "eslint": "^8.17.0",
    "prettier": "^2.7.0",
    "ts-jest": "^28.0.5",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  }
}
